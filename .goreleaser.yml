version: 2

before:
  hooks:
    - go mod download
builds:
  - id: kanban-md
    main: ./cmd/kanban-md
    binary: kanban-md
    ldflags:
      - -s -w -X github.com/antopolskiy/kanban-md/cmd.version={{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
archives:
  - id: default
    builds:
      - kanban-md
    name_template: "kanban-md_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip
brews:
  - ids:
      - default
    repository:
      owner: wyrd-company
      name: homebrew-tools
      git:
        url: git@github.com:wyrd-company/homebrew-tools.git
        private_key: "{{ .Env.FORMULAE_PUBLISH_KEY }}"
    name: kanban-md
    homepage: https://github.com/wyrd-company/kanban-md
    description: A Git-native Kanban tool powered by Markdown snapshots
    license: MIT
    install: |
      bin.install "kanban-md"
      bin.install_symlink bin/"kanban-md" => "kbmd"
      generate_completions_from_executable(bin/"kanban-md", "completion")
      generate_completions_from_executable(bin/"kbmd", "completion")
release:
  github:
  name_template: "{{.Version}}"
  prerelease: auto
  mode: append
changelog:
  disable: true
